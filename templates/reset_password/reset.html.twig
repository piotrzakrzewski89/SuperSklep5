{% extends 'base.html.twig' %}
{% trans_default_domain 'app' %}
{% block title %}
	{{parent()}}
	{{ 'page_title.reset_password_page'|trans }}
{% endblock %}

{% block body %}
	<br><h1>{{ 'reset_password.title_label'|trans }}</h1>
	{% form_theme resetForm 'bootstrap_4_layout.html.twig' %}
	{{ form_start(resetForm) }}
	{{ form_row(resetForm.plainPassword) }}
	<button class="btn btn-outline-primary">{{ 'reset_password.form.password_button'|trans }}</button>
	{{ form_end(resetForm) }}
	<br>
{% endblock %}
