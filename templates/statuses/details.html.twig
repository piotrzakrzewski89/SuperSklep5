{% extends 'base.html.twig' %}
{% trans_default_domain 'app' %}

{% block title %}
	{{ parent() }}
	{{ 'page_title.category_details_admin_page'|trans }}
{% endblock %}

{% block body %}
	<br>
	<div class="row">
		{% include 'inc/admin_panel.html.twig' %}
		<div class="card col-lg-9">
			<div class="card-header">
				<i class="fas fa-table mr-1"></i>
				Szczegóły kategorii:
				{{ statusesData.name}}
			</div>
			<div class="card-body">
				<p>cos cos</p>
			</div>
			<a href="{{ path('statuses') }}" class="btn btn-outline-danger">{{ 'form.back_button_label'|trans }}</a>
		</div>

	</div>
	<br>
{% endblock %}
