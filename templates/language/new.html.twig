{% extends 'base.html.twig' %}
{% trans_default_domain 'app' %}

{% block title %}
	{{parent()}}
	{{ 'page_title.langauge_new_page'|trans }}
{% endblock %}

{% block body %}
	<br>
	<h1>{{ 'langauge_form.title_label'|trans }}</h1>
	{% form_theme languageForm 'bootstrap_4_layout.html.twig' %}
	{{ form_start(languageForm) }}
	{{ form_row(languageForm.name) }}
	{{ form_row(languageForm.code) }}
	<div class="form-inline">
		{{ form_row(languageForm.save, {'attr': {'class': 'btn btn-outline-primary'}}) }}
		{{ form_row(languageForm.save_add_next, {'attr': {'class': 'btn btn-outline-success'}}) }}
		<a href="{{ path('language') }}" class="btn btn-outline-danger">{{ 'form.cancel_button_label'|trans }}</a>
	</div>
	{{ form_end(languageForm) }}
	<br>
{% endblock %}
